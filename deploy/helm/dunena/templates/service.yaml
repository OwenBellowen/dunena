apiVersion: v1
kind: Service
metadata:
  name: {{ include "dunena.fullname" . }}
  labels:
    {{- include "dunena.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.config.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "dunena.selectorLabels" . | nindent 4 }}
